// Точка входа приложения. Режим сверки с макетом остался в lib/main.dart:
//   flutter run                       — приложение
//   flutter run -t lib/main.dart      — все 57 кадров как в прототипе
import 'package:flutter/material.dart';

import 'app/app.dart';

void main() => runApp(const HouseKgzAppScope());
