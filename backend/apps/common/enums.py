"""Перечисления, общие для всего домена.

Перечисления каталога (тип недвижимости, продавец, статус объявления, тип медиа,
валюта) живут в `apps/catalog/enums.py` — там же, где модели, которые их используют.
"""

from django.db import models


class WalletEntryKind(models.TextChoices):
    """Тип операции по кошельку — вкладки экрана «История пополнения и трат»."""

    TOPUP = "topup", "Пополнение"
    SPEND = "spend", "Списание"
    BONUS = "bonus", "Бонусы"
