# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
venv/
ENV/
env.bak/
venv.bak/
*.sqlite3
db.sqlite3

# Flutter / Dart
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.packages
build/

# OS
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Test artifacts and screenshots
*.png
# Иллюстрации экранов входа — часть сборки, на них ссылается pubspec.yaml.
# Без исключения свежий клон не собирается: ассета нет, а Flutter его требует.
!flutter_app/assets/login/**
*.mp4
Safa-app-backend/
# Чужой проект, лежит рядом только для сверки — в House не попадает.
reference app/
backend/sample_videos/

# Isolated feature workspaces
.worktrees/

# Ключ SDK Finik для локальных сборок — в репозиторий не попадает.
flutter_app/.finik.local.json

# Креды Firebase и прочие секреты прода. Каталог существует только на сервере;
# в репозиторий не попадает ничего из него — включая пример, чтобы никто не
# принял заглушку за настоящий service-account.
backend/secrets/
